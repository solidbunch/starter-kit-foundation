# For setup SSL put your certificate files here.
# Make <your-app-domain.com> folder
# And put your SSL files with names <your-app-domain.com>/cert.pem and <your-app-domain.com>/privkey.pem
# Change var APP_PROTOCOL=https in your .env.type.[environment_type]
# Restart containers
